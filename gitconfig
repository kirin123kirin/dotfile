[user]
       email = kirin123kirin@gmail.com
       name = kirin123
[filter "lfs"]
       clean = git-lfs clean -- %f
       smudge = git-lfs smudge -- %f
       process = git-lfs filter-process
       required = true
[gui]
       encoding = utf-8
[core]
        autocrlf = input

[push]
  default = current

[commit]
  template = ~/.gitconfig.d/gitmessage

[color]
  ui = true
  
[include]
  path = ~/.gitconfig.d/gitalias.txt

[submodule]
	recurse = true
